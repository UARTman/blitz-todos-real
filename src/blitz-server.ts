import { setupBlitzServer } from "@blitzjs/next"
import {  } from "@blitzjs/rpc"

export const {
    api,
  /* plugins' exports */
} = setupBlitzServer({
  plugins: [
    // plugins will go here
  ],
})